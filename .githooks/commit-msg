#!/bin/sh
# Strips AI attribution trailers from the commit message. See
# tools/no-ai-attribution.mjs for what and why. Install with: npm run hooks
node "$(dirname "$0")/../tools/no-ai-attribution.mjs" --file "$1" || exit 0
