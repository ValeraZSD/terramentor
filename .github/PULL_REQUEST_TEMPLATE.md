<!-- Keep it short. What you changed and how you know it works is the whole ask. -->

## What

## Why

## How it was tested

<!-- The commands you ran and what they said. `npm test` prints a per-suite
     table; paste the summary line. If you touched a subsystem with its own
     guard suite, say which one. If a change is visible on screen, say what you
     looked at, in which theme, and on which width. -->

```
npm test
npm run check
```

## Checklist

- [ ] I have signed the CLA through the CLA Assistant bot's one-click link
      (first PR only), and I agree to license this contribution under the terms
      it sets out.
- [ ] Every commit carries a `Signed-off-by:` line (`git commit -s`).
- [ ] `npm test` and `npm run check` pass locally.
- [ ] No AGPL-incompatible code was pasted in; if a non-trivial chunk was
      written with an assistant's help, it is disclosed above.
